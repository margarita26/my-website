import React, { useEffect } from 'react';
import styled from '@emotion/styled';
import { fonts, colors, fontSizesVW } from '../../constants';

export const AboutPage: React.FC = () => {
  return (
    <div>
      <text style={{ color: 'white', fontSize: '64px' }}></text>
    </div>
  );
};

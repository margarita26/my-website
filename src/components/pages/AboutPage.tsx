import React from "react";
import styled from "@emotion/styled";
import { fonts, colors, fontSizes } from "../../constants";

export const AboutPage: React.FC = () => {
  return (
    <div> <text> ABout Page </text></div>
  );
};
